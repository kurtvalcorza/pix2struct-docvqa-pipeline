# Release status

Current status: **Release-grade — exact-blob E2E run passed on Kaggle Tesla T4** (commit `2602e66`, notebook blob `078435c42445`, `kurtvalcorza/dimer-nb2-pix2struct-docvqa` v3, 2026-09-26 UTC, 393.2 s, 10/10 post-restart code cells). The package includes validated synthetic/BYOD corpus helpers, ANLS and exact-match metrics with non-neural baselines, bounded final-decoder-block adaptation, digest-bound safetensors export/reload, and a standalone `E2E` notebook generated under DIMER Notebook Specification 2.0. On the default synthetic sample (one seeded 72 / 18 / 30 document-disjoint split, corpus digest equal to the pinned `SAMPLE_DIGEST`) the frozen and adapted models both measured held-out ANLS 1.0 and exact match 1.0 on 30 rows, against 0.2949 ANLS for the majority-answer baseline and 0.0 for the empty baseline; the adaptation delta is 0.0 because the frozen model already saturates this sample. Fresh reload parity was 8/8. These are measurements of one synthetic split on one runtime, not a DocVQA benchmark; the full record, runtime identity and output digests are in `docs/release-verification.md`. The first T4 attempt on `14f2cbd` failed on an embedded-module `_sha256` name collision, fixed in `2602e66`. Any change to the notebook blob returns the carrier to Candidate until a new exact-blob run is recorded.
